version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: unime_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data: